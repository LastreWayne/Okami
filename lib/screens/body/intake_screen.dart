import 'package:flutter/material.dart';

class IntakeScreen extends StatelessWidget {
  const IntakeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Intake — Maximize your health'),
      ),
    );
  }
}